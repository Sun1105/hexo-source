name: Deploy Hexo Blog to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: hexo-deploy
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 拉取源码
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ 设置 Node.js 环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3️⃣ 安装依赖
      - name: Install dependencies
        run: npm install   # ⚠️ 改为 npm install，保证 lock 文件同步（之前 npm ci 报错）

      # 4️⃣ 构建 Hexo 静态文件
      - name: Generate site (Hexo)
        env:
          CI: true
        run: |
          npx hexo clean
          npx hexo generate

      # 5️⃣ 部署到 GitHub Pages 仓库
      - name: Deploy to Pages (push to sun1105.github.io)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}  # ✅ 修正：内置令牌，不需要 PAT
          publish_dir: ./public
          external_repository: Sun1105/sun1105.github.io  # 目标仓库（部署仓库）
          publish_branch: main
          commit_message: "🚀 Auto deploy from blog-source via GitHub Actions"
