<!--
  category.ejs
  分类页面模板
  功能：
    - 显示分类标题
    - 遍历当前分类下所有文章
    - 渲染文章卡片列表
    - 当分类下无文章时显示提示
-->

<section class="main-section">
  <!-- 页面主容器，控制布局和样式 -->

  <!-- 分类标题 -->
  <h2 class="section-title">📘 <%= page.category %> 分类</h2>
  <!-- emoji 📘 用于视觉增强 -->
  <!-- <%= page.category %> 动态输出当前页面所属分类名称 -->
  <!-- 显示示例：📘 Java 分类 -->

  <div class="post-list">
    <!-- 文章列表容器，用于放置文章卡片 -->

    <% if (page.posts && page.posts.length) { %>
    <!-- 判断当前分类下是否有文章 -->
    <!-- page.posts → 当前页面文章数组 -->
    <!-- page.posts.length → 文章数量，大于 0 则进入循环 -->

      <% page.posts.each(function(post) { %>
      <!-- 遍历当前分类下每篇文章 post -->
      <!-- post 对象包含文章标题、路径、日期、分类、标签、摘要等信息 -->

        <%- partial('_partial/post-card', { post: post }) %>
        <!-- 调用局部模板 _partial/post-card.ejs 渲染文章卡片 -->
        <!-- 将当前文章对象 post 传入模板 -->
        <!-- <%- %> 输出 HTML，不转义，保证标签正常显示 -->

      <% }); %>
      <!-- 遍历文章结束 -->

    <% } else { %>
    <!-- 当前分类没有文章 -->

      <p>该分类下暂无文章。</p>
      <!-- 提示用户该分类暂时为空 -->

    <% } %>
    <!-- 条件判断结束 -->

  </div>
  <!-- 文章列表容器结束 -->

</section>
<!-- 页面主容器结束 -->
