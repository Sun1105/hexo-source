---
title: testcase_dto
date: 2026-03-18T04:21:11.886Z
categories: [メモ]
tags: [メモ]
---

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Python データ構造コード</title>
    <style>
        body {
            background-color: #0f172a;
            color: #e2e8f0;
            font-family: "Fira Code", Consolas, monospace;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #38bdf8;
            text-align: center;
        }
        pre {
            background: #020617;
            padding: 20px;
            border-radius: 12px;
            overflow-x: auto;
            box-shadow: 0 0 20px rgba(56, 189, 248, 0.2);
        }
        .keyword { color: #7dd3fc; }
        .string { color: #86efac; }
        .function { color: #fbbf24; }
    </style>
</head>
<body>

<h1>Python データ構造コード</h1>

<pre><code>
<span class="keyword">from</span> dataclasses <span class="keyword">import</span> dataclass, field
<span class="keyword">from</span> abc <span class="keyword">import</span> ABC, abstractmethod
<span class="keyword">from</span> typing <span class="keyword">import</span> List
<span class="keyword">import</span> re


<span class="keyword">class</span> Item(ABC):
    <span class="keyword">@abstractmethod</span>
    <span class="keyword">def</span> <span class="function">print</span>(self) -&gt; None:
        <span class="keyword">pass</span>


<span class="keyword">@dataclass</span>
<span class="keyword">class</span> DataElement(Item):
    name: str
    value: str

    <span class="keyword">def</span> <span class="function">print</span>(self) -&gt; None:
        print(f"名前：{self.name}")
        print(f"値：{self.value}")

    <span class="keyword">def</span> <span class="function">getElement</span>(self):
        <span class="keyword">return</span> self.name + ":" + self.value


<span class="keyword">@dataclass</span>
<span class="keyword">class</span> Data(Item):
    name: str
    level: int
    components: List[Item] = field(default_factory=list)

    <span class="keyword">def</span> <span class="function">add</span>(self, component: Item) -&gt; None:
        self.components.append(component)

    <span class="keyword">def</span> <span class="function">remove</span>(self, component: Item) -&gt; None:
        self.components.remove(component)

    <span class="keyword">def</span> <span class="function">get</span>(self, name: str):
        <span class="keyword">for</span> component <span class="keyword">in</span> reversed(self.components):
            <span class="keyword">if</span> component.name == name:
                <span class="keyword">return</span> component
        <span class="keyword">return</span> None

    <span class="keyword">def</span> <span class="function">getbylevel</span>(self, level: int):
        return_co = None
        <span class="keyword">if</span> level == 1:
            <span class="keyword">return</span> self
        <span class="keyword">for</span> component <span class="keyword">in</span> self.components:
            <span class="keyword">if</span> hasattr(component, "components"):
                return_co = component.getbylevel(level)
            <span class="keyword">if not</span> hasattr(component, "level"):
                <span class="keyword">continue</span>
            <span class="keyword">if</span> component.level == level:
                return_co = component
        <span class="keyword">return</span> return_co

    <span class="keyword">def</span> <span class="function">print</span>(self) -&gt; None:
        print(f"名前：{self.name}")
        <span class="keyword">for</span> component <span class="keyword">in</span> self.components:
            component.print()

    <span class="keyword">def</span> <span class="function">getElement</span>(self, level: int, hyphen: str):
        lines = ""
        title = self.name

        <span class="keyword">if</span> (
            title != ""
            <span class="keyword">and</span> len(self.components) == 1
            <span class="keyword">and</span> hasattr(self.components[0], "name")
            <span class="keyword">and</span> hasattr(self.components[0], "value")
            <span class="keyword">and</span> getattr(self.components[0], "name") == title
            <span class="keyword">and</span> str(getattr(self.components[0], "value")).lower() == "null"
        ):
            <span class="keyword">return</span> title + ":null\n"

        <span class="keyword">if</span> title != "":
            title = title + ":\n"

        patten = r"\((.*?)\)"
        index = ""

        <span class="keyword">if</span> title <span class="keyword">is not</span> None <span class="keyword">and</span> ("[" <span class="keyword">in</span> title <span class="keyword">or</span> "]" <span class="keyword">in</span> title):
            index = re.findall(patten, title)[0]
            <span class="keyword">if</span> index != "":
                title = re.sub(patten, "", title)
            <span class="keyword">else</span>:
                title = "\n"
                hyphen = "-"

        lines += title

        child_hyphen = hyphen

        <span class="keyword">for</span> component <span class="keyword">in</span> self.components:
            <span class="keyword">if</span> component <span class="keyword">is</span> None:
                <span class="keyword">continue</span>

            <span class="keyword">if</span> hasattr(component, "components"):
                s = component.getElement(component.level, child_hyphen)
            <span class="keyword">else</span>:
                s = component.name + ":" + component.value + "\n"

            <span class="keyword">if</span> s <span class="keyword">and</span> s.strip() != "":
                space = self.getSpace(component.level) <span class="keyword">if</span> hasattr(component, "level") <span class="keyword">else</span> ""
                prefix = child_hyphen
                lines += space + prefix + s

        <span class="keyword">return</span> lines

    <span class="keyword">def</span> <span class="function">getSpace</span>(self, level: int):
        <span class="keyword">if</span> level == 1:
            <span class="keyword">return</span> "   "
        <span class="keyword">if</span> level == 2:
            <span class="keyword">return</span> "     "
        <span class="keyword">if</span> level == 3:
            <span class="keyword">return</span> "       "
        <span class="keyword">if</span> level == 4:
            <span class="keyword">return</span> "        "
        <span class="keyword">if</span> level == 5:
            <span class="keyword">return</span> "         "
        <span class="keyword">return</span> ""
</code></pre>

</body>
</html>