---
title: main
date: 2026-03-18T04:15:27.979Z
categories: [メモ]
tags: [メモ]
---

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Python Script - JUnit Generator</title>
    <style>
        body {
            background-color: #0f172a;
            color: #e2e8f0;
            font-family: "Fira Code", Consolas, monospace;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #38bdf8;
            text-align: center;
        }
        pre {
            background: #020617;
            padding: 20px;
            border-radius: 12px;
            overflow-x: auto;
            box-shadow: 0 0 20px rgba(56, 189, 248, 0.2);
        }
        .keyword { color: #7dd3fc; }
        .string { color: #86efac; }
        .function { color: #fbbf24; }
        .comment { color: #64748b; }
    </style>
</head>
<body>

<h1>JUnit 自動生成スクリプト（Python）</h1>

<pre><code>
<span class="keyword">import</span> os
<span class="keyword">import</span> logging

<span class="keyword">from</span> constants <span class="keyword">import</span> *
<span class="keyword">from</span> CommonUtils <span class="keyword">import</span> CommonUtils
<span class="keyword">from</span> ai_create_junit <span class="keyword">import</span> *
<span class="keyword">import</span> toYaml


<span class="keyword">def</span> <span class="function">main</span>():
    cur_path = os.getcwd()

    default_level = logging.INFO
    <span class="keyword">match</span> LOG_LEVEL.lower():
        <span class="keyword">case</span> <span class="string">"debug"</span>:
            default_level = logging.DEBUG
        <span class="keyword">case</span> <span class="string">"warning"</span>:
            default_level = logging.WARNING
        <span class="keyword">case</span> <span class="string">"error"</span>:
            default_level = logging.ERROR

    CommonUtils.setupLogger(os.path.join(cur_path, LOG_DIRECTORY), default_level)
    CommonUtils.writeLog(logging.INFO, <span class="string">"------------------------------"</span>)
    CommonUtils.writeLog(logging.INFO, <span class="string">"Junit生成処理を開始します。"</span>)
    CommonUtils.writeLog(logging.DEBUG, <span class="string">f"【ENDPOINT】 - {CHAT_DEPLOYMENT} に接続します。"</span>)

    <span class="keyword">try</span>:
        in_path = os.path.join(cur_path, INPUT_DIRECTORY)
        pcl_files = CommonUtils.get_files_by_extension(in_path, <span class="string">".xlsx"</span>)

        <span class="keyword">if not</span> pcl_files:
            CommonUtils.writeLog(
                logging.ERROR,
                <span class="string">f"【{in_path}】に処理対象PCLがありません。処理を終了します。"</span>,
            )
            exit()

        total = len(pcl_files)
        count = 0

        <span class="keyword">for</span> pcl_file <span class="keyword">in</span> pcl_files:
            count += 1
            CommonUtils.writeLog(logging.INFO, <span class="string">f"PCL処理件数 {count}/{total} 処理中"</span>)

            folder_path = os.path.dirname(pcl_file)

            CommonUtils.writeLog(
                logging.DEBUG,
                <span class="string">f"フォルダ【{folder_path}】からソースファイルを読み込み、マージします。"</span>,
            )

            <span class="keyword">try</span>:
                merged_sources = CommonUtils.merge_files(
                    folder_path,
                    SRC_FILE_PATTERN,
                    ENCODING_JAVA_SOURCE
                )
                CommonUtils.writeLog(
                    logging.DEBUG,
                    <span class="string">f"merged_sources length: {len(merged_sources)}"</span>
                )
            <span class="keyword">except</span> Exception <span class="keyword">as</span> e:
                CommonUtils.writeLog(
                    logging.WARNING,
                    <span class="string">f"ソースファイルのマージ処理中にエラーが発生しました：{e}"</span>,
                )
                <span class="keyword">continue</span>

            <span class="keyword">if not</span> merged_sources:
                CommonUtils.writeLog(
                    logging.WARNING,
                    <span class="string">f"【{folder_path}】にソースファイルがありません。"</span>
                )
                <span class="keyword">continue</span>

            CommonUtils.writeLog(
                logging.DEBUG,
                <span class="string">f"【{pcl_file}】をYAML形式に変換します。"</span>
            )

            pcl_yaml, class_name, method_name = toYaml.create_yaml(pcl_file)
            CommonUtils.writeLog(logging.DEBUG, <span class="string">f"pcl_file:\n{pcl_yaml}"</span>)

            method_name = <span class="string">f"{method_name[0].upper()}{method_name[1:]}"</span>

            junit_file_name = <span class="string">f"{class_name}{method_name}Test.java"</span>
            out_path = os.path.join(cur_path, OUTPUT_DIRECTORY)
            junit_file_path = os.path.join(out_path, junit_file_name)

            ai_create_junit(
                class_name,
                method_name,
                pcl_yaml,
                merged_sources,
                junit_file_path
            )

        CommonUtils.writeLog(logging.INFO, <span class="string">"Junit生成処理が終了しました。"</span>)

    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:
        <span class="keyword">import</span> traceback

        CommonUtils.writeLog(
            logging.ERROR,
            <span class="string">f"Junit生成処理中にエラーが発生しました：{e}"</span>
        )
        CommonUtils.writeLog(logging.ERROR, str(e))
        traceback.print_exc()


<span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:
    main()
</code></pre>

</body>
</html>