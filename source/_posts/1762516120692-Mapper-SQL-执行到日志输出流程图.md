---
title: 📝 Mapper SQL 执行到日志输出流程图
category: Mybatis
tags: [log, mapper, 流程图]
date: 2025-11-27T14:06:02.734Z
---

<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <title>MyBatis SQL 执行流程图</title>

  <!-- Mermaid JS -->
  <script type="module">
    import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.esm.min.mjs';
    mermaid.initialize({ startOnLoad: true });
  </script>

  <style>
    body {
      font-family: "Segoe UI", "Microsoft YaHei", sans-serif;
      margin: 40px;
      background-color: #f9f9f9;
      line-height: 1.8;
    }
    h1, h2 {
      color: #2c3e50;
    }
    .mermaid {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      margin-bottom: 40px;
      white-space: pre-line;
    }
    code {
      background: #eee;
      padding: 2px 6px;
      border-radius: 4px;
    }
    pre {
      background: #eee;
      padding: 12px;
      border-radius: 6px;
      overflow-x: auto;
    }
  </style>
</head>

<body>

  <h1>MyBatis SQL 执行流程（可视化版）</h1>

  <h2>📌 流程图（Mermaid 渲染）</h2>

  <div class="mermaid">
flowchart TD
    A[调用 Mapper 方法] --> B[MyBatis Executor\\n核心执行器]
    B --> C[生成 SQL 语句模板\\n(带 ? 占位符)]
    C --> D[日志阶段 1\\nPreparing: SELECT ... WHERE ... = ?]
    D --> E[参数绑定\\n绑定方法参数到 ?]
    E --> F[日志阶段 2\\nParameters: 300000000 (String), 20250601 (String)]
    F --> G[执行 SQL\\nJDBC 调用 Connection → PreparedStatement → executeQuery/executeUpdate]
    G --> H[获取执行结果\\nSELECT → 结果集\\nUPDATE/INSERT → 影响行数]
    H --> I[日志阶段 3\\n执行结果: Total = 1]
    I --> J[返回 Mapper 方法结果]
  </div>

  <h2>📘 详细步骤说明</h2>

  <h3>1. 调用 Mapper 方法</h3>
  <p>用户通过 Mapper 接口调用对应方法。</p>

  <h3>2. MyBatis Executor 核心执行器</h3>
  <p>MyBatis 核心执行器负责整个 SQL 执行流程。</p>

  <h3>3. 生成 SQL 语句模板</h3>
  <p>根据 Mapper XML 或注解生成带 <code>?</code> 占位符的 SQL。</p>

  <h3>4. 日志打印阶段 1: Preparing</h3>
  <pre>
==> Preparing: SELECT ... WHERE ... = ?
  </pre>

  <h3>5. 参数绑定</h3>
  <p>将方法参数绑定到 SQL 中的 <code>?</code> 占位符。</p>

  <h3>6. 日志打印阶段 2: Parameters</h3>
  <pre>
==> Parameters: 300000000 (String), 20250601 (String)
  </pre>

  <h3>7. 执行 SQL</h3>
  <p>通过 JDBC 调用：</p>
  <ul>
    <li>Connection</li>
    <li>PreparedStatement</li>
    <li>executeQuery() / executeUpdate()</li>
  </ul>

  <h3>8. 获取执行结果</h3>
  <ul>
    <li><strong>SELECT</strong> → 返回结果集</li>
    <li><strong>UPDATE / INSERT</strong> → 返回影响行数</li>
  </ul>

  <h3>9. 日志打印阶段 3: 执行结果</h3>
  <pre>
<== Total: 1
  </pre>

  <h3>10. 返回 Mapper 方法结果</h3>
  <p>将结果返回给调用方。</p>

</body>
</html>
